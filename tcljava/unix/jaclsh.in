#!/bin/sh
# Wrapper script to start up Jacl

# Do not edit this script unless it is called jaclsh.in.
# configure takes jaclsh.in, substitutes the @xx@
# fields and creates jaclsh.

# Author:  Moses DeJong
# Version: %Id%
#
# Copyright (c) 1998 by Moses DeJong
# 	All Rights Reserved, but you can use this file.

# The next line gets substituted by configure
XPLATFORM_LIB_INSTALL_DIR=@prefix@/lib

# Add the .jar files to the CLASSPATH
JACL_CLASSPATH=${XPLATFORM_LIB_INSTALL_DIR}/tcljava.jar:${XPLATFORM_LIB_INSTALL_DIR}/jacl.jar:@JAVA_CLASSPATH@

JAVA=@JAVA_DIR@/bin/java

# Start Java runtime with 5 Meg Min Heap and 20 Meg Max Heap
JAVA_FLAGS="@JAVA_FLAGS@ -ms5m -mx20m"

# Run java with the args passed in from the calling environment
t
exec ${JAVA} ${JAVA_FLAGS} -classpath ${JACL_CLASSPATH} tcl.lang.Shell ${1+"$@"}
